diff --git a/node_modules/react-native-vision-camera/ios/Core/CameraSession+Photo.swift b/node_modules/react-native-vision-camera/ios/Core/CameraSession+Photo.swift
index 12f5c07..97de56f 100644
--- a/node_modules/react-native-vision-camera/ios/Core/CameraSession+Photo.swift
+++ b/node_modules/react-native-vision-camera/ios/Core/CameraSession+Photo.swift
@@ -50,6 +50,9 @@ extension CameraSession {
 
       // depth data
       photoSettings.isDepthDataDeliveryEnabled = photoOutput.isDepthDataDeliveryEnabled
+      if photoOutput.isCameraCalibrationDataDeliverySupported {
+        photoSettings.isCameraCalibrationDataDeliveryEnabled = true
+      }
       if #available(iOS 12.0, *) {
         photoSettings.isPortraitEffectsMatteDeliveryEnabled = photoOutput.isPortraitEffectsMatteDeliveryEnabled
       }
diff --git a/node_modules/react-native-vision-camera/ios/Core/PhotoCaptureDelegate.swift b/node_modules/react-native-vision-camera/ios/Core/PhotoCaptureDelegate.swift
index e845b38..009e138 100644
--- a/node_modules/react-native-vision-camera/ios/Core/PhotoCaptureDelegate.swift
+++ b/node_modules/react-native-vision-camera/ios/Core/PhotoCaptureDelegate.swift
@@ -62,6 +62,32 @@ class PhotoCaptureDelegate: GlobalReferenceHolder, AVCapturePhotoCaptureDelegate
       let cgOrientation = CGImagePropertyOrientation(rawValue: exifOrientation) ?? CGImagePropertyOrientation.up
       let orientation = getOrientation(forExifOrientation: cgOrientation)
       let isMirrored = getIsMirrored(forExifOrientation: cgOrientation)
+      var cameraCalibrationData: [String: Any]? = nil
+      if let calibration = photo.cameraCalibrationData {
+        let k = calibration.intrinsicMatrix
+        let referenceDimensions = calibration.intrinsicMatrixReferenceDimensions
+        let fx = Double(k.columns.0.x)
+        let fy = Double(k.columns.1.y)
+        let cx = Double(k.columns.2.x)
+        let cy = Double(k.columns.2.y)
+        cameraCalibrationData = [
+          "source": "AVCapturePhoto.cameraCalibrationData",
+          "referenceDimensions": [
+            "width": Double(referenceDimensions.width),
+            "height": Double(referenceDimensions.height),
+          ],
+          "fx": fx,
+          "fy": fy,
+          "cx": cx,
+          "cy": cy,
+          "K": [
+            [fx, 0.0, cx],
+            [0.0, fy, cy],
+            [0.0, 0.0, 1.0],
+          ],
+          "K_row_major": [fx, 0.0, cx, 0.0, fy, cy, 0.0, 0.0, 1.0],
+        ]
+      }
 
       promise.resolve([
         "path": path.absoluteString,
@@ -71,6 +97,7 @@ class PhotoCaptureDelegate: GlobalReferenceHolder, AVCapturePhotoCaptureDelegate
         "isMirrored": isMirrored,
         "isRawPhoto": photo.isRawPhoto,
         "metadata": photo.metadata,
+        "cameraCalibrationData": cameraCalibrationData as Any,
         "thumbnail": photo.embeddedThumbnailPhotoFormat as Any,
       ])
     } catch let error as CameraError {
diff --git a/node_modules/react-native-vision-camera/lib/typescript/types/PhotoFile.d.ts b/node_modules/react-native-vision-camera/lib/typescript/types/PhotoFile.d.ts
index 2e4c2f2..ca09394 100644
--- a/node_modules/react-native-vision-camera/lib/typescript/types/PhotoFile.d.ts
+++ b/node_modules/react-native-vision-camera/lib/typescript/types/PhotoFile.d.ts
@@ -67,6 +67,19 @@ export interface PhotoFile extends TemporaryFile {
      */
     isMirrored: boolean;
     thumbnail?: Record<string, unknown>;
+    cameraCalibrationData?: {
+        source: string;
+        referenceDimensions: {
+            width: number;
+            height: number;
+        };
+        fx: number;
+        fy: number;
+        cx: number;
+        cy: number;
+        K: number[][];
+        K_row_major: number[];
+    };
     /**
      * Metadata information describing the captured image. (iOS only)
      *
diff --git a/node_modules/react-native-vision-camera/src/types/PhotoFile.ts b/node_modules/react-native-vision-camera/src/types/PhotoFile.ts
index 85098d6..403ebaa 100644
--- a/node_modules/react-native-vision-camera/src/types/PhotoFile.ts
+++ b/node_modules/react-native-vision-camera/src/types/PhotoFile.ts
@@ -69,6 +69,19 @@ export interface PhotoFile extends TemporaryFile {
    */
   isMirrored: boolean
   thumbnail?: Record<string, unknown>
+  cameraCalibrationData?: {
+    source: string
+    referenceDimensions: {
+      width: number
+      height: number
+    }
+    fx: number
+    fy: number
+    cx: number
+    cy: number
+    K: number[][]
+    K_row_major: number[]
+  }
   /**
    * Metadata information describing the captured image. (iOS only)
    *
